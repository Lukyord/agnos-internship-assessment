export default function resultSymtompAbdomen(index) {
  if (index === 1) return "ช่องท้องด้านบนขวา";
  else if (index === 2) return "ลิ้นปี่";
  else if (index === 3) return "ช่องท้องด้านบนซ้าย";
  else if (index === 4) return "รอบซะดือ";
  else if (index === 5) return "ช่องท้องด้านล่างขวา";
  else if (index === 6) return "ท้องน้อย";
  else if (index === 7) return "ช่องท้องด้านล่างซ้าย";
}
